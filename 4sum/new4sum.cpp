#include <iostream>
#include <vector>
using namespace std;

vector <vector<int> > fourSum(vector<int> &nums) {
	sort(nums.begin(),nums.end());
	auto last = nums.end();
	int target = 0;

	for(auto i = nums.begin(); i < last - 3; i++ ){
		
		for(auto j = i+1; j <)

	}
};